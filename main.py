# from Embedding import Embedding, PositionalEmbedding
# import matplotlib.pyplot as plt
# import torch

# emb = Embedding(100, 512)
# print(emb.embed)

# plt.figure(figsize=(20, 10))
# po = PositionalEmbedding(100, 512)
# y = po.forward(torch.autograd.Variable(torch.zeros(100, 512)))
#
# cax = plt.matshow(po.pe)
# plt.gcf().colorbar(cax)
# plt.show()
